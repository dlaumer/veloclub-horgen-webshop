name = "veloclub-gas-proxy"
main = "src/worker.ts"
compatibility_date = "2024-09-01"

# Optional: later, you can bind this to your domain with routes like:
# routes = [
#   { pattern = "api.yourdomain.com/api/*", zone_name = "yourdomain.com" }
# ]
